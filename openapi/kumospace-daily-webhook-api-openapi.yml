openapi: 3.2.0
info:
  title: '@kumo/api Daily (Webhook) API'
  contact: {}
servers:
- url: /
tags:
- name: Daily (Webhook)
paths:
  /v1/integrations/daily/webhook:
    post:
      operationId: HandleDailyWebhook
      responses:
        '204':
          description: No content
      tags:
      - Daily (Webhook)
      security: []
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              type: object
      summary: Handle daily webhook
      x-summary-source: derived
components:
  securitySchemes:
    firebase:
      type: http
      scheme: bearer
      bearerFormat: JWT
